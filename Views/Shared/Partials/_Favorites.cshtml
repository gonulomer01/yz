<section id="section-favorites" class="page">
      <div class="glass-card dash-section-card">
        <div class="dash-section-header" style="justify-content: space-between;">
          <h3 style="color:var(--text-main); margin:0;"><i class="fa-solid fa-heart" style="color:var(--color-primary);"></i> Favoriler</h3>
          <button class="action-btn primary-btn" onclick="openAddImagesToCollectionModal('__favorites__')">
            <i class="fa-solid fa-plus"></i> Görsel Ekle
          </button>
        </div>
        <div id="favorites-grid" class="gallery-grid-main"></div>
      </div>
    </section>